apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nginx-ssl-test.fullname" . }}-test-connection"
  labels:
    {{- include "nginx-ssl-test.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nginx-ssl-test.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
