apiVersion: v1
kind: Service
metadata:
  name: {{ include "nginx-ssl-test.fullname" . }}
  labels:
    {{- include "nginx-ssl-test.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nginx-ssl-test.selectorLabels" . | nindent 4 }}
